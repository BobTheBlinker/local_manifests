<?xml version="1.0" encoding="UTF-8"?>
<manifest>
  <!-- A-Team private signing keys -->
  <project path="vendor/ateam"
           remote="private"
           name="BOBtheBlinker/ateam_signing_keys"
           revision="main"
           clone-depth="1" />

  <!-- Build Environment Signing
    export ANDROID_SIGNING_KEYS=vendor/ateam
    export PRODUCT_DEFAULT_DEV_CERTIFICATE=vendor/ateam/releasekey
    export OTA_PACKAGE_SIGNING_KEY=vendor/ateam/releasekey
    export SIGNING_KEYS_DIR=vendor/ateam
  -->
</manifest>
